# Server-only — never NEXT_PUBLIC_ for the API key or backend URL secrets.
# Browser calls /api/*; Next proxies to FastAPI with X-API-Key.

TELCO_API_URL=http://localhost:8000
TELCO_API_KEY=dev-local-telco-key
